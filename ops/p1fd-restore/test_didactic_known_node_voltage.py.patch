diff --git a/tests/test_didactic_known_node_voltage.py b/tests/test_didactic_known_node_voltage.py
index 3797213..495c633 100644
--- a/tests/test_didactic_known_node_voltage.py
+++ b/tests/test_didactic_known_node_voltage.py
@@ -40,12 +40,12 @@ def _divisore(v_term: tuple[str, str], amount: Fraction) -> IR:
 
 
 def _fino_al_riferimento(ir: IR):
-    _, d1 = applica_passo("choose_reference", ir, stato_iniziale(NODO))
+    _, d1 = applica_passo("choose_reference", ir, stato_iniziale(NODO), operands=())
     return d1
 
 
 def _fino_alle_incognite(ir: IR):
-    _, d2 = applica_passo("define_nodal_unknowns", ir, _fino_al_riferimento(ir))
+    _, d2 = applica_passo("define_nodal_unknowns", ir, _fino_al_riferimento(ir), operands=())
     return d2
 
 
@@ -306,7 +306,7 @@ def test_binding_duplicata_fail_closed():
         _generatori_verso_riferimento(ir)
     d1 = _fino_al_riferimento(ir)
     with pytest.raises(ValueError, match="due generatori verso riferimento"):
-        applica_passo("define_nodal_unknowns", ir, d1)
+        applica_passo("define_nodal_unknowns", ir, d1, operands=())
